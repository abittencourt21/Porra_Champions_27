[project]
name = "porra-champions-league"
version = "0.1.0"
description = "Motor y front estático para la porra de la UEFA Champions League."
requires-python = ">=3.12"
dependencies = [
  "google-api-python-client>=2.0",
  "google-auth>=2.0",
]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
